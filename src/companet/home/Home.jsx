import React from 'react';
import {isTSIntersectionType} from "eslint-plugin-react/lib/util/ast.js";

const Home = () => {
    return (
        <section>

        </section>
    );
};

export default Home;